use_pkgdown()

use_pkgdown_github_pages()

Tools/Terminal/New terminal

git status

git log

git add .Rbuildignore .gitignore DESCRIPTION _pkgdown.yml .github/

git add .github/.gitignore

git status

git commit -m "Build initial pkgdown website for package"

git push

Para crear el sitio y guardarlo en github

build_readme()
build_site()
